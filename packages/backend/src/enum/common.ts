
export enum BaseSort {
    id = "id",
    createAt = "createAt",
    updatedAt = "updatedAt"
}

export enum Order {
    DESC = "DESC",
    ASC = "ASC"
}

export enum CardType {
    INLINE = "INLINE",
}